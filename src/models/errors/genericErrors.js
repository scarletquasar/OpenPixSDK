const genericErrors = {
    "fetchError": "[OpenPixSDK] An errour ocurred while trying to fetch data, see more info in the previous message",
    "requiredFieldRequired": "[OpenPixSDK] Validation error, the following field is required: "
}

export { genericErrors }